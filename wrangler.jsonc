{
  "name": "lnnserver", // 你的站点名称（自定义）
  "compatibility_date": "2025-12-24", // 兼容日期（按提示填当前日期）
  "assets": {
    "directory": "./dist" // 静态资产目录指向dist
  },
  // 可选：指定部署环境（默认production）
  "env": {
    "production": {
      "routes": ["https://it.lnn.ink/*"] // 绑定自定义域名（可选）
    }
  }
}
